# Copy this file to .env and fill in your values.
# Use forward slashes (/) even on Windows.

# Where raw and processed data files are stored.
# Should be OUTSIDE the project folder (e.g., a shared Dropbox folder).
DATA_DIR=D:/Dropbox/your-project-name

# Where tables and figures are saved. Default is the output/ folder.
OUTPUT_DIR=output
